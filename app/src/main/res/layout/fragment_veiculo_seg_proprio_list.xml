<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context=".presenter.proprio.veiculoseg.VeiculoSegProprioListFragment">

    <TextView
        android:id="@+id/text_view_veiculo_seg"
        style="@style/text_view_title_list"
        android:text="@string/texto_veiculo_seg" />

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/list_view_veiculo_seg"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        app:layoutManager="androidx.recyclerview.widget.LinearLayoutManager"
        tools:listitem="@layout/item_row_text" />

    <Button
        android:id="@+id/button_inserir_veiculo_seg"
        style="@style/botao_avulso"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/texto_inserir" />

    <LinearLayout
        android:id="@+id/layout_botoes"
        android:layout_width="match_parent"
        android:layout_height="wrap_content" >

        <Button
            android:id="@+id/button_canc_veiculo_seg"
            style="@style/botao_avulso"
            android:layout_weight="1"
            android:layout_width="fill_parent"
            android:layout_height="wrap_content"
            android:text="@string/texto_padrao_cancelar" />

        <Button
            android:id="@+id/buttonOkVeiculoSeg"
            style="@style/botao_avulso"
            android:layout_weight="1"
            android:layout_width="fill_parent"
            android:layout_height="wrap_content"
            android:text="@string/texto_padrao_ok" />

    </LinearLayout>

</LinearLayout>